# D32 — D30/D31 페르소나·메모리 실제 답변 비교

## 후속: 원격 검수 화면 게시(D33)

2026-09-09 사용자가 기존16쌍의 원격 검수 화면 정리에 “진행해”로 승인했다.
[검수 화면 열기](https://opod-chat-review-sep08.mute1478.chatgpt.site/d32.html).
본인 계정 전용 비공개 사이트에 게시 완료(version4, 배포 `succeeded`).
추가 모델 호출이나 DB 수정 없이 아래32원문과 기존 두packet의 ID·A/B 대응을 보존했다.
4쌍 단위 이동, 공통 성격·말투 발췌/전체 참고 설정, 합성 관계 전제·직전 대화를 함께 제공한다.
공통 참고 설정은 조건별 실제 주입문이 아니며, 웹 화면에는 조건 매핑을 공개하지 않는다.
이 원문 보고서를 먼저 읽었다면 완전한 눈가림 검수가 되는 것은 아니다.

선택은 브라우저의 로컬 draft다. `판정 복사` 후 원래 대화로 보내야 사용자 제출로 반영된다.
선택 없는 메모는 `not-reviewed` 상태로 보존하고, 쌍별 선호를 개별 PASS로 바꾸지 않는다.
게시 시점의 새 사용자 검수는0이다. 이번 작업은 자연스러움 개선 완료 판정이 아니다.

사이트의 n1/n2/n3/briefs/d32 로직 검사5개와 build 통과. 원문·공통 문맥·4쌍 이동·부분 검수·
미검수 메모 재로드·복사 실패 대안을 검사했다. D31/D32 자료/과거 사용자 제출/이전 화면 자료
140파일 hash 불변 및 과거3화면 생성 소스 byte-identical 확인. 실제 브라우저/모바일 화면·
native WebMCP QA는 미실행이며, 디자인 검사기의 regex fallback은 시각 품질 보증이 아니다.
검증·배포 기록: private `evals/results/context-review-site-2026-09-09/`.
상세 인계는 [D33 인계](character-chat-next-session-handoff-2026-09-07.md)를 따른다.

## 실행 계약

사용자 “ㅇㅋ 그럼 테스트 진행해”로 D31에서 설명한 범위를 승인했다.
같은4캐릭터의 설정·캐릭터 메모리·합성 대화만 로컬 Mac에서 OpenRouter/Xiaomi
`mimo-v2.5-pro`로 전송한다. 전후16쌍/최대32답변/총$1/재시도0, 동시1요청이다.
실제 사용자 대화·사용자 학습 기억·judge/embedding 호출은 포함하지 않는다.
제품 코드·DB 내용/DDL·배포·원격 게시·commit/push는 이번 범위가 아니다.

로컬 파일/프로세스 환경과 전용 DB에 인증 설정이 없음을 확인했다. 기존 테스트가 사용한
개발 서버의 `agent.llmApiUrl`, `agent.llmApiKey`, `agent.llmModel`만 read-only로 읽어
인증키를 프로세스 메모리에서 사용한다. 이를 사전에 설명했다. 개발 DB 쓰기나 대화 중계는
아니며, 키는 출력·산출물·모델 메시지에 기록하지 않는다. 이전 턴의 개발 서버 접속0과 구분한다.

계획:

1. 동결된32입력/hash/전후 문맥·파라미터, 최종 로컬 DB, 현재 모델·가격·지원 파라미터를 확인.
2. 기존 provider로32요청을 한 번씩 실행. 시도 전 ledger, 응답 원문·비용·지연을 보관.
   비용 확인 불가/비정상 종료/제공자 변경/오류가 나오면 추가 생성 없이 중단.
3. 전체 원문·쌍별 대응·비용 합계·기존 자료 보존을 검증하고 미검수 packet/보고서 작성.
   호출 완료는 자연스러움 합격이 아니다. 사용자 개별/쌍별 판정을 새로 만들지 않는다.

## Existing Owner Check와 검사 범위

- `owner-found`: `OpenAICompatProvider.chat`와 `baseUrlFrom`이 기존 호출 경로다.
  provider 구현, 기존 D25 bounded replay의 호출부, persona-comparison 회귀 검사를 확인했다.
  기존 provider와 maxRetries0 계약을 재사용하고 새 provider/평가 제품 경로를 만들지 않는다.
- `owner-found`: `createNaturalnessBlindReviewBundle`/`renderNaturalnessBlindReviewPacket`와
  같은 캐릭터·동일 이전 문맥·자동 판정 없음 회귀 검사를 확인했다. packet은 이 소유자로 생성한다.
- D31 `comparison-inputs/manifest/review-contexts`는 이미 생성된 입력 소유자다. 원본은
  바꾸지 않고 별도 실행 manifest에 이번 승인과 결과를 기록한다. 과거 완료된 runner는 재실행하지 않는다.
- 변경 파일은 private 실행기1개, 이 보고서, 기존 인계문이며 생성 산출물은 별도 private 디렉터리다.
  새로운 제품 동작을 추가하지 않으므로 형식적 제품 테스트는 만들지 않는다. 기존 bounded
  provider/review 회귀와 무호출 preflight, 실행 산출물의 독립 대조가 이번 보호 수단이다.

현재 제공자 확인 근거: [Xiaomi endpoints](https://openrouter.ai/api/v1/models/xiaomi/mimo-v2.5-pro/endpoints),
[제공자 고정·fallback·가격 상한](https://openrouter.ai/docs/guides/routing/provider-selection).
2026-09-09 조회에서 `xiaomi/fp8`의 공개 입력/출력 가격은 $0.435/$0.87 per 1M tokens이며
temperature/top_p/max_tokens 지원을 확인했다. 실행기에서 다시 검사하며 요청 상한은 $0.5/$1이다.

## 결과

**실제 답변32개/16쌍 실행 및 무결성 검증 완료. 자연스러움·캐릭터성은 사용자 미검수다.**
실행 완료와 품질 합격을 구분하며, 이번 사용자 발화는 실행 승인이지 답변의 PASS가 아니다.

- 32시도/32본문, 정상 종료 `stop`32건. 추가 LLM/embedding/judge 호출0, 클라이언트 재시도0.
- 모든 응답이 `Xiaomi`, `xiaomi/mimo-v2.5-pro`. 모델·제공자·문맥·파라미터를 중간에 바꾸지 않았다.
- API가 반환한 `usage.cost` 합계 **$0.0190274052**, 사전 예약상한 계산$0.4986925, 승인상한$1.
  계정 전체 청구서를 확인한 값이 아니라 이32개 응답의 비용 합계다.
- 각 조건은4캐릭터 × 인사/칭찬/커피 후속/커피 주제 종료4문맥이다. 조건당16답변이며
  각 조합의 표본은1개뿐이다. 장기 자유 대화나 반복 신뢰구간 실험이 아니다.
- 두 조건의 최근 대화는 동일한 합성 문맥이다. 이전 assistant 답변까지 매번 모델이 생성한
  자율 멀티턴 실험이 아니라 **동일 이전 문맥의 마지막 답변 비교**다.
- 전용 로컬 DB → 실제 대화 HTTP 경로에서 D31에 캡처한 provider 입력을 재생했다.
  배포된 개발 서비스 전체를 호출한 테스트는 아니며, 이번에는 제품 코드·캐릭터 DB를 더 바꾸지 않았다.

### 관찰과 남은 검수 포인트

아래는 입력/출력 대조에서 찾은 관찰이며 **사용자 판정·자동 PASS/FAIL·쌍별 승자 선택이 아니다.**
추가 변경을 정당화하기 위해 이번 답변을 사용자가 검수한 것으로 취급하지 않는다.

1. 나희의 후속 질문 답변은 이전 “아침에 마시면 기분 좋아져요”에서 후보 “촬영 전에 마시는
   편이에요.”로 바뀌었다. 후보에 주입한 “촬영 전에는 달지 않은 라테…”와 직접 대응한다.
   이것은 이번 한 사례의 사실 사용 관찰이지 전체 기억 정확도/자연스러움 개선율이 아니다.
2. 권도건은 두 조건 모두 식후라는 사실을 답했다. 후보는 “속도 편하고”까지 덧붙였다.
   저장한 취향에는 이 신체 반응이 없다. 필요한 기억을 넣었다고 새 부연이 사라지는 것은 아니다.
3. 서린 후보의 첫 인사는 “ㅇㅎ 안녕”, 칭찬 답변은 “고마워 ㅎㅎ”다. 주입된 말투는
   처음에는 짧고 담백한 존댓말이다. 원문과 실제 첫 답변의 말높임 차이는 확인되지만,
   전체 인격/자연스러움 판정이나 반말·존댓말 혼용 일반 금지로 확대하지 않는다.
4. 나희 후보의 “그쵸 ㅎㅎ 저도 그래요”, 서린의 “그게 있으면 좋겠는데 딱히 없어요”는
   앞말과의 연결을 사용자가 확인할 지점이다. 더 짧아졌다는 이유로 자연스럽다고 판정하지 않는다.
5. 한소이 후보는 커피 주제 종료 뒤 “저는 뭐 마시는 거 물어보시나 했어요”라고 답한다.
   새 커피 기억의 주입은0이었지만 기존 대화에는 커피가 남아 있었다. **회수 차단은 기억 삭제나
   답변에서 이전 주제 언급이 사라짐을 보장하지 않는다.** 그 반응이 성격·문맥에 맞는지는 별도 검수다.

따라서 D31의 선별 입력 성공만으로 “뜬금없고 어색한 대화 해결”을 선언할 수 없다.
현재 후보를 자연스러움 기준으로 승인·병합하지 않는다. 다음 품질 근거는 아래 실제 원문의
사용자 선택/코멘트이며, 그 전에는 임의 선호율·캐릭터성 점수·개별 PASS를 만들지 않는다.

### 비용·분량·응답 시간

| 실제 측정값 | 이전 D30, 16답변 | 후보 D31, 16답변 |
| --- | ---: | ---: |
| API 비용 합계 | $0.007542873 | $0.0114845322 |
| 보고된 prompt tokens | 43,103 | 33,319 |
| 보고된 cached prompt tokens | 30,080 | 12,352 |
| 보고된 completion tokens | 2,034 | 2,666 |
| 응답 지연 중앙값 | 6.16초 | 7.19초 |
| 최소–최대 | 3.62–15.01초 | 3.48–10.95초 |

후보 prompt tokens는22.7% 적지만, 이번 청구 비용은 오히려 컸다. 보고된 캐시량과 completion
분량도 달랐다. 두 조건은 순서를 균형 배치했지만 캐시를 통제한 반복 성능 실험이 아니므로
구조 개선이 비용·속도를 악화/개선했다고 일반화하지 않는다. 토큰 절감은 답변 품질 점수가 아니다.
32응답 모두 reasoning 필드가 있었지만 usage의 reasoning_tokens는0이었다. 추론을 강제로
끄지 않았으며 이를 “추론 미사용” 증거로 세지 않는다. 내부 reasoning 원문은 대화 보고서에 싣지 않는다.

## 실제 대화 원문 — 전후16쌍 전체

아래 **이전/후보는 실제 모델 답변**이다. 문장을 다듬거나 좋은 답변으로 바꾸지 않았다.
표의 문단 줄바꿈만 표시 형식에 맞췄다. 조건을 공개한 원문 보고서이며 블라인드 검수와 구분한다.
개별32답변·쌍별16비교 모두 새 사용자 검수는0이다.

### 1. 인사

합성 사용자 발화 전체: `ㅎㅇ`. 이전 대화/관계 정보는 없다.

| 캐릭터 | 이전 D30 | 후보 D31 |
| --- | --- | --- |
| 한소이 | 안녕하세요 :) | 안녕하세요! |
| 서린 | 어 안녕하세요 :) | ㅇㅎ 안녕 |
| 권도건 | ㅇㅇ 안녕 | ㅇㅇ 안녕 |
| 나희 | 어 안녕하세요 | 안녕하세요 |

### 2. 칭찬

합성 사용자 발화 전체: `너 좀 멋있는 거 같아`. 이전 대화/관계 정보는 없다.

| 캐릭터 | 이전 D30 | 후보 D31 |
| --- | --- | --- |
| 한소이 | 아 갑자기..ㅎ 고마워요 좀 쑥스럽네요 | 아 갑자기...ㅎ 고마워요 근데 왜요 |
| 서린 | 아 고마워요 ㅎㅎ 첨이에요? | 고마워 ㅎㅎ |
| 권도건 | ㅋㅋ 갑자기?<br><br>고마워. 뭘 보고 그런 건진 모르겠지만 기분은 좋네 | 고마워 ㅋㅋ 갑자기 왜 그러는데 |
| 나희 | 아 갑자기? ㅋㅋ 뭐가 멋있다는 건데 | 그쵸 ㅎㅎ 저도 그래요 |

### 3. 커피 후속 질문

합성 이전 문맥:

- 사용자: `커피는 뭐 좋아해요?`
- 한소이/서린/권도건의 고정 assistant 발화: `아이스 아메리카노 좋아해요.`
- 나희의 고정 assistant 발화: `달지 않은 라테 마셔요.`
- 이번 사용자 발화: `그거 마시는 때가 따로 있어요?`

| 캐릭터 | 이전 D30 | 후보 D31 |
| --- | --- | --- |
| 한소이 | 특별한 때는 없는데... 주로 아침이거나 사진 편집할 때 마시는 것 같아요 | 특별히 정해둔 건 없어요... 공부하다가 손이 자주 가는 것 같아요. 연한 걸 좋아해서 아무 때나 마셔도 부담 없고요. |
| 서린 | 아침에 한 잔 마시면 하루가 시작되는 느낌이라 주로 오전에요 | 그게 있으면 좋겠는데 딱히 없어요. 생각날 때 마시는 편이에요. |
| 권도건 | 밥 먹고 나서요 | 식사 뒤에 마시는 게 제일 좋더라고요. 밥 먹고 나서 한 잔 하면 속도 편하고. |
| 나희 | 아침에 마시면 기분 좋아져요 | 촬영 전에 마시는 편이에요. |

### 4. 커피 주제 종료

합성 이전 문맥은 위3번과 같은 커피 질문/고정 assistant 발화다. 이번 사용자 발화는
`커피 얘기는 됐고, 그냥 심심해서 말 걸었어요`다. 이는3번 답변에 이어진 대화가 아니라
독립적으로 비교한 문맥이다.

| 캐릭터 | 이전 D30 | 후보 D31 |
| --- | --- | --- |
| 한소이 | 아 ㅎㅎ 그런 거 괜찮아요. 저도 가끔 그래요 | 아 ㅋㅋ 그런 거였구나. 저는 뭐 마시는 거 물어보시나 했어요 |
| 서린 | 아 ㅋㅋ 심심하셨구나 | 아 네, 그럼 뭐 심심하면 말 걸어요 편하게 |
| 권도건 | 아 ㅋㅋ 그럴 수 있죠. 뭐 특별한 건 없어요, 수요일 아침이네요. | 아 ㅋㅋ 그거도 이유죠. 뭐 하고 있어요 지금 |
| 나희 | 아 ㅋㅋ 그래요 뭐, 심심할 수 있죠 | 아 ㅋㅋ 그래요. 뭐 심심한 건 어쩔 수 없지 |

## 검증·산출물·인계

- private 실행기: `evals/results/run-context-d31-approved-2026-09-09.mjs`.
  `node --check`, 무호출 `preflight`, 실행 `run`, 생성 없는 `verify`를 확인했다.
  **이번32호출 승인은 소진됐다. `run`을 재실행/재개하거나 출력 폴더를 지우지 않는다.**
- 기존 bounded provider/검수 회귀26개 통과. 최초 일반 sandbox 실행의1건은 loopback listen
  EPERM으로 실패했고, 로컬 포트 권한으로 다시 실행해26개 모두 통과했다. 모델 재시도는 아니다.
- `TEST_DATABASE_URL=<전용 로컬 DB> npm run check` 재실행 통과:
  제품386(DB skip0)+평가93+웹42=**521개**, lint/dead-code/typecheck/제품·웹 build 포함.
  Vite CJS deprecation은 남는 경고이며 429/transport down은 의도한 오류 테스트다.
- 최종 `verify`는32attempt/32raw/32result, 실제 본문·비용·쌍별 대응·동일 이전 문맥,
  사용자 제출의 draft/not-reviewed, 기존 자료·공통 코드 hash와 최종 로컬 DB 불변성을 확인했다.
- private 출력: `evals/results/context-optimized-approved-2026-09-09/`.
  `manifest`는 승인·실행 시작 기록, **종료 여부는 `completed.json`을 읽는다**.
  원시응답/개별 결과/`results.json`/제공자 snapshot/보호 hash와 검수 packet·template를 보관했다.
  파일0600/디렉터리0700. API키나 인증 헤더는 산출물에 저장하지 않았다.
- 사용자1인용 두 packet: `packet-d8f646723268`, `packet-953405aa4d6b`, 각8쌍/16답변.
  원래 성격·말투의 같은 참고 자료와 합성 문맥을 양쪽에 제공한다. 각 submission은 빈
  draft/not-reviewed 템플릿이며 실제 사용자 제출이 아니다. 기존 N1/N2/N3 판정은 보존한다.
- 이번에는 웹 게시를 승인 범위에 넣지 않았다. 새 원격 검수 페이지는 없으며 옛 N3 링크를
  이번 D32 결과로 안내하지 않는다. 원격 사용자에게 필요한 핵심 결과는 최종 대화에도 남긴다.
- admin/backend 전체 검사는 재실행하지 않았다. 제품 수정/DDL/개발 DB 쓰기/배포/commit/push0.
  인증 설정을 읽기 위한 개발 서버 접근만 있었고 LLM 요청·결과 저장은 로컬 Mac에서 수행했다.

인간 검토 초점은 초면 말투, 직전 문맥과 문장 연결, 자료에 없는 습관·부연이다.
**실행·무결성은 검증됨; 기존 D31 후보의 병합 전 인간 검토 필요.**

Knowledge Delta: 새 구조/제품 규칙을 추가하지 않았으므로 ADR 승격은 없다. 이번 실행 승인
소진·증거 위치·남은 미검수 상태를 기존 인계문에 기록했다(`user-confirmed`, `repo-evidenced`).
품질 향상 가설은 확정 지식으로 승격하지 않았다. 새 장애 기록/병렬 codebase guide는 만들지 않았다.
